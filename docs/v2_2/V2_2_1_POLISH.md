# V2.2.1 界面与地图交互收尾

本轮根据 V2.2 实机评审反馈完成以下收尾，不扩展 V2.3 系统范围。

## 系统菜单

- 系统菜单宽度调整为 244 像素。
- 最后一项缩短为“返回主菜单”，不再使用越界的长文案。
- “返回主菜单”现在实际切换到 `res://scenes/menu/main_menu.tscn`。

## 联系人

联系人不再由界面写死为让娜。

- 联系候选来自当前人物的正式关系记录。
- 候选名称读取 `target_display_name_zh`，缺失时依次回退到原文名和稳定 ID。
- 皮埃尔当前可联系让娜·勒鲁瓦。
- 阿尔贝没有对应关系记录，因此不显示让娜，也不能安排该联系。
- 联系活动、人物关系页、通知和日程标签使用同一关系数据源。
- 无联系人时显示“当前人物没有可用的关系行动”。

## 安排活动面板

- 打开面板后权威时间暂停。
- 关闭面板后恢复打开前的暂停状态和倍率。
- 暂停变化立即刷新时间显示。
- 日期、开始、结束和持续时长集中到一个带边框的时间编辑区。
- 调整按钮缩短为“日− / 日+ / 时− / 时+ / 时长− / 时长+”。
- 联系活动表单保存真实 `related_entity_id` 和显示名称。

## 地图边缘滚动

V2.2 主场景新增鼠标边缘滚动：

- 有效边缘宽度：20 像素。
- 平移速度按鼠标接近边缘的程度在 240—720 像素/秒之间变化。
- 右边缘向右移动镜头，左边缘向左移动，上下边缘同理。
- 位于 UI 控件上、按下鼠标或正在拖动地图时不触发。
- 边缘滚动复用地图已有 `begin_camera_interaction` / `pan_by` / `end_camera_interaction`，不会重建地图几何。

## 验证入口

新增：

```text
res://tests/v2_2/v2_2_polish_test.gd
```

统一验证 `tools/run_validation.ps1` 已包含该回归。在线修改阶段没有执行本地 Godot 测试，需拉取后运行现有 V2.2 完整验收。
