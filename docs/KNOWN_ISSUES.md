# 已知问题

## P0 可玩性复审

- 当前真实状态：后台模拟、状态一致性、关闭开发模式的玩家核心闭环和 Windows 11 正式 UI 已通过本轮验收；P0-R1 阶段性健全，Windows 11 Release 冒烟通过。

- M0 至 M9 的历史记录只作参考；当前结论来自 2026-07-15 重构后的统一回归、实际窗口和独立 EXE 复验。
- 2026-07-13 Codex 使用精确 Godot `4.6.3.stable.official.7d41c59c4` 审计了提交 `9e92e77c422be782fd21baaa69ee7b41099ce8be`。原始 Headless 进程虽然返回 `0`，日志包含强类型解析错误，因此实际不通过。
- 审计后的默认分支已修复该编译阻断，并加入自举验证脚本：先执行 Headless 编辑器导入生成全局类缓存，再运行全部测试，同时扫描解析和脚本加载错误。
- 核心成长闭环已补充：正式学习可选择任意人物技能，非学习行动产生主技能实践成长；主技能达到精通、准备和资金均达到配置阈值时，公式提供明确的保证成功上界。
- 继承退出原因已增加权威约束：退休、死亡、长期监禁和严重失势必须由年龄、健康、拘禁、声望或明确状态证明；自愿退出保持可用。
- NPC 日结算已改为先按旧效率结算已过去区间，再从日边界应用新目标、权限和状态；不再使用日末新状态追溯整天。
- 存档恢复已增加活跃人物上限、人物层级、激活种子完整性、活跃 NPC 的 AI 全覆盖，以及玩家/NPC 行动实例 ID 全局唯一性校验。
- 1280×720 行动面板的正式开始按钮已固定在滚动区外；备份恢复测试改为按 JSON 语义归一化比较。
- 新增 `tests/codex_audit_regression.gd`，覆盖上述审计项并对普通与完整人口模式共 1000 种种子验证核心行动保证成功可达性。

## 2026-07-15 UI 重构前验证基线

- 在精确 Godot `4.6.3.stable.official.7d41c59c4` 上运行 `tools/run_validation.ps1`，全部步骤退出码为 `0`，日志没有解析、类型或脚本加载错误。
- 综合 `570/570`、逻辑 `35/35`、自动玩家旅程 `32/32`、安全 `26/26`、状态一致性 `41/41`、模拟质量 `50/50`、Codex 审计专项 `29/29`。
- 普通与完整人口模式共 1000 种种子的保证成功可达性通过；年轻健康人物伪造退出、NPC 新状态追溯旧时间、存档 AI/种子/上限/行动 ID 不变量均有绿色回归。
- 30 日与一年长期模拟通过；目标规模一年约 `2,833 ms`，测试存档约 `292.7 KiB`，日/月/保存单次结算均低于 100 ms 预算。
- 满活跃层继承和继承失败事务回滚通过；外部存档仍严格执行 20 人活跃上限。

## 2026-07-15 UI 重构后当前证据

- 统一验证通过综合 `572/572`、逻辑 `35/35`、1280×720 可见控件旅程 `95/95`、安全 `26/26`、状态一致性 `41/41`、模拟质量 `50/50`、Codex 审计 `30/30`；无解析或脚本加载错误。
- 1280×720 实际窗口关闭开发者模式完成新游戏、八类行动首屏、学习与成长、暂停/继续、Space/Esc、人物页、社会入口、保存退出加载。
- Windows x86-64 Release 单 EXE 标题不含 `DEBUG`，已从 PowerShell、资源管理器及项目目录外临时位置独立启动；保存加载与正常退出通过。

## 当前设计限制

- 设置界面仍处于禁用状态；地图仍是程序绘制的正交架空占位地理。
- 主动投入只使用人物财富；情报点仍是自动准备因素，不是单独可消耗资源。
- 技能成长、精通保证、组织收入、生命周期和控制压力权重均为首版值，需要长期模拟和平衡测试。
- 活跃 NPC 只从有限规则候选中选择，不运行复杂外交、市场或完整战争计划。
- NPC 的月度组织地区行动仍是组织层级结算，不为每次组织决策创建人物行动实例。
- 玩家满足退休或死亡条件时会设置继承要求，但尚未增加强制遮罩或自动弹出继承界面。
- 控制压力同时读取地区 `social_influence` 和单元 `social_support`，两层权重尚未经过长期平衡测试。
- 继承最多新增两项组织身份并保留继承者原有身份，具体继承比例仍是首版值。
- 背景人物保存完整成长核心，新增 NPC 行动和资金审计后的当前测试存档约 `292.7 KiB`；仍需关注长期存档增长。
- 旧版本 1 存档缺少部分新字段时的兼容路径仍需真实旧档往返验证。
- Windows 10、Linux 和 macOS 尚无当前版本实机证据；Windows 11 冒烟不能外推到这些平台。
- 开发者面板在 1280×720 下信息较密，长文本仍可能裁切。

当前准确状态是：P0-R1 阶段性健全，Windows 11 Release 冒烟通过。仍未完成的限制包括真实旧档迁移、Windows 10 外部实机、Linux/macOS 导出、安装程序/签名和长期平衡；本项目当前产物是便携式单 EXE，不是安装程序。
